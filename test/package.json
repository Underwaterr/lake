{
  "type": "module",
  "scripts": {
    "test": "reset && node --test",
    "test:only": "reset && node --test --test-name-pattern"
  },
  "engines": {
    "node": "=22"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
